import { Box } from '@chakra-ui/react'
import styled from '@emotion/styled'

export const BioSection = styled(Box)`
  padding-left: 3.4em;
  text-indent: -3.4em;
  margin-bottom: 1em;
`

export const BioHeader = styled(Box)`
  display: flex;
  gap: 2.4em;
`

export const Company = styled(Box)`
  font-weight: bold;
  text-indent: -3.4em;
`

export const Year = styled.span`
  font-weight: bold;
  margin-right: 1em;
`
